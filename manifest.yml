---
applications:
  .:
    name: moodyfriends
    framework:
      name: rack
      info:
        mem: 128M
        description: Rack Application
        exec: 
    infra: aws
    url: ${name}.${target-base}
    mem: 128M
    instances: 2
    services:
      postgres:
        type: postgresql
