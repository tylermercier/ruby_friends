require 'bundler/setup'
require 'sinatra/activerecord/rake'
#require 'rspec/core/rake_task'
#require 'pry'
require './app'

#RSpec::Core::RakeTask.new(:spec)

# task :default => :spec

# desc "start server on port 3000"
# task :server do
#   sh 'bundle exec shotgun -p 3000'
# end

# desc "start debugging console"
# task :console do
#   binding.pry
# end
